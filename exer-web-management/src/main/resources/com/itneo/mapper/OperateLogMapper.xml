<!--定义Mapper映射文件的约束和基本结构-->
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itneo.mapper.OperateLogMapper">

    <!--分页查询OperateLog信息-->
    <select id="list" resultType="com.itneo.pojo.OperateLog">
        select
            op.id, op.operate_emp_id, op.operate_time, op.class_name, op.method_name, op.method_params, op.return_value, op.cost_time, e.name operate_emp_name
        from operate_log op left join emp e on op.operate_emp_id = e.id
    </select>

</mapper>